export interface RecruiterProfilePropTypes {
  about: string;
  empSize: string;
  location: string;
  name: string;
  preferredIndustry: string;
  site: string;
  skills: string;
  whatsappNumber: string;
}

export interface ProfilePropsTypes {
  profileDetails: any;
  fetchProfileDetails: any;
  loading: boolean;
}
