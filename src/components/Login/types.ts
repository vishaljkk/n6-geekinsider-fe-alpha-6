import { StateUITypes } from '../../types';

export interface LoginPropsTypes {
	// setUiState?: StateUITypes,
	setUiState?: any,
	onChange?: any,
	signIn?: any,
	formType?: string,
	signUp?: any,
	userType?: any, 
	setUserType?: any
}